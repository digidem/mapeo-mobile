import React from "react";
import { View } from "react-native";

const GpsModalScreen = () => (
  <View
    style={{
      flex: 1,
      backgroundColor: "rgba(0,0,0,0.85)"
    }}
  />
);

export default GpsModalScreen;
