import { TouchableHighlight } from "react-native";

export { Touchable, TouchableHighlight, TouchableOpacity } from "react-native";

const TouchableNativeFeedback = TouchableHighlight;

export { TouchableNativeFeedback };
